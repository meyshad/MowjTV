# راه‌اندازی روی هاست

فولدر `public/tv` منبع اصلی و مستقل سایت و فولدر `release/tv` نسخهٔ بسته‌بندی‌شدهٔ آن است.

1. فایل `release/tv.zip` را در ریشه عمومی هاست (معمولاً `public_html`) آپلود کنید.
2. آن را همان‌جا Extract کنید تا مسیر `public_html/tv/index.html` ساخته شود.
3. آدرس `https://your-domain.com/tv/` را در مرورگر تلویزیون باز کنید.

نیازی به PHP، دیتابیس، Node.js یا تنظیمات سرور نیست. فقط لازم است هاست فایل‌های استاتیک را نمایش دهد و صفحه با HTTPS باز شود.

## کنترل روی تلویزیون

- بالا/پایین: حرکت بین شبکه‌ها
- Enter: انتخاب و پخش
- تمام‌صفحه: از دکمه زیر پلیر
- در تمام‌صفحه، کنترل‌ها روی تصویر نمایش داده می‌شوند و پس از چند ثانیه کنار می‌روند
- دکمه‌های «شبکه قبلی» و «شبکه بعدی»: تعویض مستقیم شبکه در تمام‌صفحه
- Back: خروج از تمام‌صفحه؛ خارج از تمام‌صفحه رفتار عادی مرورگر حفظ می‌شود
- Channel Up/Down: تعویض شبکه در تمام‌صفحه، در صورت ارسال این کلیدها توسط مرورگر

## نکته پخش

فهرست شبکه‌ها از IPTV-ORG و پخش از سرور خود هر شبکه دریافت می‌شود. بعضی شبکه‌ها ممکن است به‌دلیل خاموش بودن لینک، محدودیت منطقه‌ای، CORS یا HTTP ناامن در مرورگر تلویزیون پخش نشوند؛ این محدودیت از سمت منبع است و یک سایت کاملاً استاتیک نمی‌تواند آن را دور بزند.

سنجش دسترسی به‌صورت سبک و خودکار در پس‌زمینه انجام می‌شود: در فهرست‌های بزرگ حداکثر ۴۰ شبکه در هر بار استفاده و در هر نوبت فقط یک فایل کوچک فهرست پخش بررسی می‌شود. هنگام کندی، بافر کم یا توقف تصویر، سنجش فوراً کنار می‌رود. پخش‌های متوقف‌شده نیز با فاصله‌های افزایشی دوباره متصل می‌شوند و در حالت کیفیت خودکار، سقف کیفیت از ۴۸۰p به ۳۶۰p و سپس ۲۴۰p کاهش پیدا می‌کند؛ البته فقط اگر منبع شبکه چنین کیفیتی ارائه کند.

فهرست‌های دریافت‌شده، از جمله فهرست بزرگ «همه شبکه‌ها»، داخل فضای ذخیره‌سازی بزرگ مرورگر تلویزیون نگه‌داری می‌شوند تا در قطع موقت اینترنت هم آخرین نسخه باز شود. شش دستهٔ اخیراً استفاده‌شده نگه داشته می‌شوند و کش سبک‌تر وضعیت پخش، سقف کنترل‌شده‌ای دارد تا فضای تنظیمات و علاقه‌مندی‌ها پر نشود. پاک‌کردن داده‌های مرورگر تلویزیون، بازنشانی دستگاه یا محدودیت شدید فضای ذخیره‌سازی می‌تواند این کش محلی را حذف کند.

این پروژه هیچ ویدئویی را میزبانی یا بازپخش نمی‌کند. فقط از پخش‌هایی استفاده کنید که مجاز به مشاهدهٔ آن‌ها هستید؛ جزئیات حقوقی و اجزای بیرونی در `README.md` و `THIRD_PARTY_NOTICES.md` آمده است.
